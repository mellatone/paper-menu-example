{{#paper-menu as |menu|}}
  {{#menu.trigger}}
    {{#paper-button iconButton=true}}
      {{paper-icon "local_phone"}}
    {{/paper-button}}
  {{/menu.trigger}}
  {{#menu.content width=4 as |content|}}

    {{#content.menu-item onClick=(action (mut noop))}}
      {{paper-icon "adb"}}
      <span>This is enabled.</span>
    {{/content.menu-item}}

    {{#content.menu-item onClick=(action (mut noop))}}
      {{paper-icon "adb"}}
      <span>This is enabled.</span>
    {{/content.menu-item}}

    {{#content.menu-item onClick=(action (mut noop))}}
      {{paper-icon "adb"}}
      <span>This is enabled.</span>
    {{/content.menu-item}}

    {{#content.menu-item onClick=(action (mut noop))}}
      {{paper-icon "adb"}}
      <span>This is enabled.</span>
    {{/content.menu-item}}

  {{/menu.content}}
{{/paper-menu}}
